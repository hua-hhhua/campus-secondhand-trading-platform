spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_trade?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&connectTimeout=5000&allowPublicKeyRetrieval=true
    username: root
    password: 156435
    druid:
      initial-size: 1
      min-idle: 1
      max-active: 5
      max-wait: 3000
      validation-query: SELECT 1
      test-on-borrow: true
      test-while-idle: true
      time-between-eviction-runs-millis: 60000

  thymeleaf:
    cache: false
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8

  messages:
    basename: i18n/messages
    encoding: UTF-8

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  data:
    redis:
      host: localhost
      port: 6379
      password:

  mail:
    host: smtp.163.com
    port: 465
    username: 18071545193@163.com
    password: ZUScp35hVTuyiAMw
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: smtp.163.com
          timeout: 10000
          connectiontimeout: 10000
          writetimeout: 10000

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

  mvc:
    throw-exception-if-no-handler-found: true

  jackson:
    time-zone: Asia/Shanghai
  jdbc:
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    lazy-loading-enabled: true
    aggressive-lazy-loading: false
  global-config:
    db-config:
      id-type: auto

file:
  upload:
    path: D:/campus_trade_files

blog:
  notification:
    emails: 18071545193@163.com

logging:
  level:
    org.springframework.security: INFO
    com.campus.trade.config.UserContextInterceptor: DEBUG